<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:WPFApp"
        xmlns:av="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="av" x:Class="WPFApp.MainWindow"
        Title="QLNV" Height="600" Width="1000">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <local:SideMenu Grid.Column="0"/>

        <Frame x:Name="MainContentFrame" Grid.Column="1" Margin="20"/>

        <StatusBar Grid.ColumnSpan="2" VerticalAlignment="Bottom">
            <StatusBarItem Content="Nhân viên: ADMIN"/>
            <StatusBarItem Content="Quyền hạn: Quản trị hệ thống"/>
            <StatusBarItem Content="Ngày: 01/25/2024" HorizontalAlignment="Right"/>
        </StatusBar>
        <Button Grid.Column="1" Content="Add Leave Request" HorizontalAlignment="Left" Margin="20,20,0,0" VerticalAlignment="Top" Height="21" Width="173" Click="Button_Click"/>
        <DataGrid Grid.Column="1" x:Name="ViewLeaveRequest"  Margin="20,73,89,348" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Employee ID" Binding="{Binding Employee.EmployeeId}" Width="*"/>
                <DataGridTextColumn Header="Leave Type" Binding="{Binding LeaveType}" Width="*"/>
                <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate}" Width="*"/>
                <DataGridTextColumn Header="End Date" Binding="{Binding EndDate}" Width="*"/>
                <DataGridTextColumn Header="Status"  Binding="{Binding Status}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <local:SideMenu Grid.Column="1" HorizontalAlignment="Left" Height="4" Margin="400,584,0,0" VerticalAlignment="Top" Width="4"/>
    </Grid>
</Window>