<Window x:Class="WPFApp.NotificationManagement"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Notification Management" Height="500" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>

        <!-- Notifications List -->
        <DataGrid x:Name="NotificationDataGrid" AutoGenerateColumns="False"
                  Grid.Column="0" Margin="10" SelectionChanged="NotificationDataGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding NotificationId}" Width="50"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="200"/>
                <DataGridTextColumn Header="Date" Binding="{Binding CreatedDate, StringFormat=MM/dd/yyyy}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Notification Details -->
        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Notification Details" FontWeight="Bold" Margin="0,0,0,10"/>

            <TextBlock Text="Title:"/>
            <TextBox x:Name="TitleTextBox" IsReadOnly="True" Margin="0,0,0,10"/>

            <TextBlock Text="Content:"/>
            <TextBox x:Name="ContentTextBox" IsReadOnly="True" AcceptsReturn="True" Height="100" Margin="0,0,0,10"/>

            <TextBlock Text="Created Date:"/>
            <TextBox x:Name="CreatedDateTextBox" IsReadOnly="True" Margin="0,0,0,10"/>
        </StackPanel>
    </Grid>
</Window>
